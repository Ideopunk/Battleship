import React from "react";


function No(props) {
	return (
		<div class="no">
			This game employs functionality that is not supported by mobile browsers. To become a
			hero, return on a computer.
		</div>
	);
}

export default No;
